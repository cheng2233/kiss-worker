{
  "name": "kiss-worker",
  "version": "1.0.0",
  "description": "KISS Translator sync service for Cloudflare Workers",
  "type": "module",
  "packageManager": "npm@10.9.2",
  "devDependencies": {
    "wrangler": "^4.24.3"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put AUTH_VALUE"
  }
}
